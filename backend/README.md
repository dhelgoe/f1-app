# F1 Trivia Backend

1. Install dependencies:
   cd backend
   npm install

2. Set up your MongoDB connection string and JWT secret in the .env file.

3. Start the server:
   npm run dev

API Endpoints:
- POST /api/auth/register (username, email, password)
- POST /api/auth/login (email, password)
